import type { ReactElement } from "react";

export const MapPage = (): ReactElement => {
  return (
    <div>
      <h1>Map</h1>
      <p>Interactive map with lost and sighting reports goes here.</p>
    </div>
  );
};